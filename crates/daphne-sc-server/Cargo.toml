[package]
name = "daphne-sc-server"
edition.workspace = true
version.workspace = true
build = "build.rs"

[dependencies]
daphne-sc-core = { path = "../daphne-sc-core" }
anyhow = "1"
prost = "0.14"
zmq = "0.10"

[build-dependencies]
prost-build = "0.14"
